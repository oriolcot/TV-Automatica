name: Actualitzar Futbol

on:
  schedule:
    - cron: '0 */4 * * *' # Executar cada 4 hores
  workflow_dispatch: # Botó manual

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Baixar el codi
        uses: actions/checkout@v3

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 📦 Instal·lar llibreries
        run: pip install requests

      - name: 🚀 GENERAR WEB I ACTUALITZAR MEMÒRIA
        env:
          API_URL: ${{ secrets.API_URL }}
        # Redirecció màgica: El que surt per print() va a index.html
        run: python generator.py > index.html

      - name: 💾 Guardar i Pujar (Web + Memòria)
        run: |
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "action@github.com"
          # Guardem tant la web com la base de dades JSON
          git add index.html memoria_partits.json
          git commit -m "Auto-update ⚽" || echo "Sense canvis nous"
          git push